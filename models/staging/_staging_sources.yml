version: 2

sources:
  - name: online_retail
    description: "Raw online_retail transaction data"
    schema: raw
    tables:
      - name: online_retail_data
        description: "Original sales records from the online_retail store"
        columns:
          - name: invoice_no
            description: "Invoice number"
            tests:
              - not_null
          - name: customer_id
            description: "Unique customer ID"
